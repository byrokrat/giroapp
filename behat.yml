default:
    suites:
        default:
            contexts:
                - FeatureContext:
                    debug: false
        debug:
            contexts:
                - FeatureContext:
                    debug: true
