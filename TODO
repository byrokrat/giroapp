# TODO

Issues om
    Fruktansvärt förvirrande med både flysystem och filesystem, styr upp det...

    Setup namespace borde bort helt och hållet...

6. INI-grejjen

    * Skriv om issue med senaste tankar. Måste inte fixa just nu...

## Att vid configure veta vilka filter/formats vi har..


5. Sedan följer hur plugins ska laddas.

    * OrgBankgiroFactory och de andra prylarna ligger inte så bra i Utils
        vart ska de vara någonstans???

    * dokumentera i docs/plugins.md

7. Load filters and formatters in configure

    det kanske blir automatiskt med FilterContainer osv ????

```php
class Plugin implements PluginInterface
{
    private $objects;

    public function __construct(object ...$objects)
    {
        $this->objects = $objects;
    }

    public function loadPlugin(Environment $environment): void
    {
        foreach ($this->objects as $objectToLoad) {
            if ($objectToLoad instanceof CommandInterface) {
                $environment->registerCommand($objectToLoad);
            }
            // ...
        }
    }
}

// gör att en kan skriva

return new Plugin(
    new class extends XmlFormInterface {
        public function getTranslations(): array
        {
            // osv...
        }
    }
);
```
